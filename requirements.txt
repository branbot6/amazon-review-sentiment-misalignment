# corpus_stats.py and the test suite need none of these — standard library only.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
vaderSentiment>=3.3.2
matplotlib>=3.7
seaborn>=0.13

# Only for regenerating predict_stars (see data/README.md).
torch>=2.0
transformers>=4.35
nltk>=3.8
